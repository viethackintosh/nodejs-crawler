import express from 'express';
import categoryController from '../controllers/posts.controller.js';

const route = express.Router();

//route.get('/posts',categoryController.getPost)
//route.post('/posts',categoryController.posts)
//route.get('/',categoryController.getPosts)
//route.post('/',categoryController.post)

export default route;
